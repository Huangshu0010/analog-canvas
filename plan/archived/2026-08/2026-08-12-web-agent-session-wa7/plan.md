---
status: completed
experience: none
---

# WP-WA7 — Delivery Hardening and Rollout Validation

## Goal

Validate the local portion of the delivery gate for the web Agent session work
(WP-WA0–WA6) and record exactly what remains for remote CI and deployment, so
the branch is in a deliverable state and the mainline gate is unambiguous.

## State and Ownership

Start state: clean `codex/web-agent-session-architecture` after WP-WA6 (`d575ea2`),
except a `pnpm-lock.yaml` Prettier fix (the lockfile must stay Prettier-clean,
like `main`).

Owned paths:

- `pnpm-lock.yaml` (Prettier reformat — restores format:check)
- `plan/2026-08-12-web-agent-session-wa7/plan.md`, one `plan/log.md` entry
- `docs/roadmap/web-agent-session-integration-plan.md` (exit-gate status)

## Local gate verified

Run from a clean dependency state on `codex/web-agent-session-architecture`:

- `corepack pnpm install --frozen-lockfile` — consistent; no lockfile drift.
- `corepack pnpm format:check` — clean (after the lockfile reformat).
- `corepack pnpm references:check` — 4 pinned reference entries validated.
- `corepack pnpm typecheck` — clean.
- `corepack pnpm test` — 108 files / 656 tests pass.
- `corepack pnpm agent-api:artifacts:check` — 3 Agent API artifacts valid (WP-WA6).

This covers the static + unit portions of `ci:check`.

## Remote / deployment gate NOT verifiable in this environment

Honestly deferred — they require the user, CI runners, and a deployment that
this environment cannot provide:

- GitHub Actions required checks on a pushed review branch (the authoritative
  green result for the mainline gate).
- `ci:release` and `ci:e2e` (editor production build + Playwright), including
  the WP-WA5 Playwright grant→claim→edit→undo→revoke and project-replacement
  flows.
- The Cloudflare `AgentSession` Durable Object + authenticated browser WebSocket
  channel + `worker/index.ts` route wiring + wrangler `AGENT_SESSION` binding
  (the WP-WA4 transport shell), deployed and exercised end to end.
- Performance budgets at 100/500-instance Snapshot/render/relay and the
  concurrency/dedupe/dropped-response/dropped-payload scenarios.
- Relay no-Project-retention verification after session cleanup on the deployed
  review environment.

Every authorization, idempotency, expiry, scope, and limit decision in the relay
is delegated to the WP-WA4 state machine, which IS deterministically tested; the
deferred items are transport, deployment, and product-scale validation.

## Validation

- `git diff --check`, `git status --short --branch`
- the local gate above (already green)

## Commit Intent

```text
chore(agent): format lockfile and record delivery gate (WP-WA7)
```

## Outcome

Restored `pnpm-lock.yaml` to Prettier-compliance (it had been regenerated by the
WP-WA3/WA5 non-frozen installs in pnpm's default format) and recorded the
delivery gate. The local gate is fully green on the branch: frozen-lockfile
install, `format:check`, `references:check`, `typecheck`, and the full unit
suite (108 files / 656 tests), plus `agent-api:artifacts:check`. The remote CI
required checks, the Playwright e2e flows, the Cloudflare Durable Object +
WebSocket transport deployment, and the performance/no-retention validation
remain the user's mainline delivery gate and are documented as such.
